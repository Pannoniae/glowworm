{game_entity
	(include "/properties/car.ext")
	(include "/properties/selection/vehicle_elongated.inc" scale(1.10))
	(include "/properties/resupply.inc")
	{props "truck" "vision_lev02" "tractor" "fuel_tank"  "Captor"}
	{able "emit_on_attack"}
	{extension "bedford_oyc_fuel.mdl"}
	{collider "truck"}
	{patherId "blitz36"}

	{extender "enumerator"
		{digit_folder "us2"}
	}

	("wheel" args "wheelright1")
	("wheel" args "wheelleft1")
	("wheel" args "wheelright2")
	("wheel" args "wheelleft2")

	("glass" args "glass1")
	("glass" args "glass2")
	("glass" args "glass3")
	("glass" args "glass4")
	("fuel" args "fuel1")
	("fuel" args "fuel2")
	("transmission" args "transmission")

	("bodyback" repair(30))
	
	{volume "detail5" {thickness 10}}
	{volume "body2" {able {holed}}}
	{volume "bodyback1" {tags "fuel"}}

	("supply_fuel" liters(2840))

	("crew_2_human")

	("board_animation"
		place(driver)
		board_begin(board_big-midcar)
		board_end(seat_driver_car)
		base(doorright)
	)
	("board_animation"
		place(commander)
		board_begin(board_big-midcar_driver)
		board_end(seat_passenger)
		base(doorleft)
	)

	{extender "inventory"
		{box
		}
	}

	{mass 6968}
	{Chassis
		("car_trace" fx("tracewheel03") step(0.08) wheell("wlf") wheelr("wrf") bonel("FXtraceL1") boner("FXtraceR1"))
		("car_trace" fx("tracewheel03d") step(0.08) wheell("wlr") wheelr("wrr") bonel("FXtraceL2") boner("FXtraceR2"))
		("mobility_car"
			speed(65) ;----------------------------------Max speed.
			speedatmaxturn(15) ;-------------------------Max speed at max turn.
			reverse(22) ;--------------------------------Max speed in Reverse.
			weight(6.968) ;-------------------------------Weight in Tonnes.
			power(72) ;----------------------------------Horse Power.
			fuel(145) ;----------------------------------Fuel capacity in Litres.
			type(fuel) ;---------------------------------Fuel type.
			range(450) ;---------------------------------Range in Kilometers.
		)
	}
}

