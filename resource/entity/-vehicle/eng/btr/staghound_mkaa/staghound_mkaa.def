{game_entity
	(include "/properties/armoredcar.ext")
	(include "/properties/selection/vehicle.inc" scale(1.0))
	{props "tank" "turret" "wheeled" "with_gun" "vision_lev02" "open_death_fx" "shell_eject" "detect_tank_mg"}
	{extension "staghound_mkaa.mdl"}
	{extension "crew.ext"}
	(include "staghound_mkaa.ext")
	{targetclass "armoredcar"}
	{targetSelector "tank_light"}
	{patherId "sdkfz232"}

	{extender "enumerator"
		{digit_folder "us2"}
	}

	{extender "inventory"
		{box
			{item "hmgun_usa api" 2200} 		;// 2600 rounds
			{item "satchel_charge_eng" 1}
		}
	}

	{Weaponry
		("restore_ik_time")
		{place "mgun"
			{Barrels 2}
			{type "main"}
			{foresight "foresight14"}
			{gunner "gunner"}
			{charger "charger"}
			{weapon "browning_m2_x2" filling "hmgun_usa api" 400}
			("abm_mgun")
			{Rotate}
			{basic}
		}
	}

	{mass 13116}
	{Chassis "wheel"
		("car_trace" fx("tracewheel01a") step(0.1) wheell("wlf") wheelr("wrf") bonel("FXtraceL1") boner("FXtraceR1"))
		("car_trace" fx("tracewheel01a") step(0.1) wheell("wlr") wheelr("wrr") bonel("FXtraceL2") boner("FXtraceR2"))
		("mobility_btr"
			speed(88) ;----------------------------------Max speed.
			speedatmaxturn(25) ;-------------------------Max speed at max turn. 
			reverse(21) ;--------------------------------Max speed in Reverse.
			weight(13.11) ;-------------------------------Weight in Tonnes.
			power(194) ;---------------------------------Horse Power. Each engine generates (two of them are inside) 97 horsepower so 2 times 97 = 194
			fuel(370) ;----------------------------------Fuel capacity in Litres. Base fuel is 230L + Externa Fuel is 140L, so carries 370L of fuel
			type(fuel) ;-------------------------------Fuel type.
			range(720) ;---------------------------------Range in Kilometers.  Base range is 450km without external fuel, with external fuel its 720km.
		)
	}

	{bone "gun_rot"
		{limits -10 75}
		{speed2 18.0}
	}
	{bone "turret"
		("turret_light" power_traverse(55)) ;// eletric-hydaulic
	}
}
